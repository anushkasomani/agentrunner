# OpenAI (planning only)
OPENAI_API_KEY=

# Solana + Devnet
SOLANA_RPC_URL=https://wild-late-season.solana-devnet.quiknode.pro/b0ebcc50a76d22c777b9f18945f0d47e9f71ccaf
SOLANA_RPC_WEBSOCKET_URL=wss://api.devnet.solana.com
FEE_PAYER_SECRET_KEY=   # dev keypair only
RUNNER_PUBKEY=<base58 public key of runner>

# JUPITER + PYTH
JUPITER_BASE=https://lite-api.jup.ag
PYTH_HERMES_BASE=https://hermes.pyth.network

# Jito (optional; ok to leave blank)
JITO_BLOCK_ENGINE_URL=
JITO_TIP_LAMPORTS=0

# x402 Merchant (accepts USDC on devnet by default)
X402_CURRENCY=USDC
X402_PAYTO_MINT=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v   # replace with devnet USDC mint if needed
X402_PAYTO_ADDRESS=<your USDC token account on devnet>

# Data layer (benchmarks, receipts indexing, replay)
DATA_LAYER_URL=http://your-data-layer:8080

# Postgres (ratings, manifests, offers)
PG_URL=postgres://postgres:postgres@postgres:5432/agentrunner

# Redis (job queues)
REDIS_URL=redis://redis:6379

# Solana Agent Registry (Anchor)
AGENT_PROGRAM_ID=<pubkey after deploy>
AGENT_IDENTITY=<agent pubkey to attach validations to, e.g. runner pubkey for demo>
AGENT_OWNER_SECRET_KEY=[ ... JSON array of 64 bytes ... ]     # owner authority for register/update
VALIDATOR_SECRET_KEY=[ ... JSON array of 64 bytes ... ]       # who posts daily validations (can reuse fee payer in demo)
